// IMS Context stub - no longer using Adobe Identity Management Services
import { createContext } from 'react';
import type { Ims } from "../utils/IMS";

export const IMSContext = createContext<Ims | null>(null);







